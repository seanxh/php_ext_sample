// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sample", "for sample support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sample", "enable sample support", "no");

if (PHP_SAMPLE != "no") {
	EXTENSION("sample", "sample.c");
}

